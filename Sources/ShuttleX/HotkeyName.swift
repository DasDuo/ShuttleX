import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// The user-configurable global shortcut that toggles the Spotlight panel.
    /// No default is set — the user records one in Settings (empty = disabled).
    static let toggleShuttleX = Self("toggleShuttleX")
}
